using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Driver : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("I will be your Driver!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
